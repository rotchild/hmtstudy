/**
 * Created by Administrator on 15-11-10.
 */
var Schema = require('jugglingdb').Schema;
var martcar = function (schema) {
    var martcar = schema.define('martcar', {
        openid: {type: String, length: 255},
        tsmartcar: Schema.Text,
        wmmartcar: Schema.Text,
        szpmartcar: Schema.Text
    });
    return martcar;
};

module.exports = martcar;